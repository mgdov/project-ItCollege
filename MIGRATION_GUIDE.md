# 🚀 Руководство по миграции на новую архитектуру

## ✅ Что уже сделано:

### 1. Store система (Zustand-like)

- ✅ Создана система управления состоянием без useState
- ✅ 4 отдельных store: auth, game, student, admin
- ✅ Полная типизация TypeScript
- ✅ Селективная подписка на изменения (производительность!)

### 2. Shared компоненты

- ✅ 9 переиспользуемых компонентов для игр и dashboard
- ✅ DifficultySelect, VictoryScreen, GameHeader, GameStats
- ✅ PageHeader, LoadingState, EmptyState, StatCard, DataTable

### 3. Пример полной миграции

- ✅ `memory/page_new.tsx` - пример игры с новой архитектурой
- ✅ Логика шашек вынесена в `shared/games/checkers-logic.ts`

## 📋 План миграции остальных игр:

### Шаг 1: Изучить пример Memory

Откройте `app/dashboard/games/memory/page_new.tsx` - это идеальный пример новой архитектуры:

- Нет useState для игровой логики
- Используется gameStore
- Все UI компоненты переиспользуемые
- Код чистый и читаемый

### Шаг 2: Миграция каждой игры

#### A. Checkers

```bash
# 1. Логика уже вынесена в shared/games/checkers-logic.ts
# 2. Нужно переписать page.tsx по примеру Memory:

# Заменить useState на:
const difficulty = useStore(gameStore, s => s.difficulty)
const score = useStore(gameStore, s => s.score)
const isPlaying = useStore(gameStore, s => s.isPlaying)

# Использовать shared компоненты:
<DifficultySelect onSelect={handleStart} />
<GameHeader title="Шашки" onReset={reset} />
<GameStats score={score} moves={moves} />
<VictoryScreen score={score} onRestart={restart} />
```

#### B. Chess (самая большая - 1363 строки!)

```bash
# 1. Создать shared/games/chess-logic.ts
# 2. Вынести логику: движение фигур, AI, проверка шаха/мата
# 3. Создать компоненты:
#    - shared/games/chess-board.tsx
#    - shared/games/chess-piece.tsx
#    - shared/games/move-history.tsx
# 4. Использовать gameStore для состояния
```

#### C. Millionaire

```bash
# 1. Вынести вопросы в shared/games/millionaire-questions.ts
# 2. Создать компоненты:
#    - shared/games/question-card.tsx
#    - shared/games/prize-ladder.tsx
#    - shared/games/lifelines.tsx
# 3. Использовать gameStore
```

#### D. Words

```bash
# 1. Вынести словарь в shared/games/words-dictionary.ts
# 2. Создать компоненты:
#    - shared/games/word-input.tsx
#    - shared/games/word-hints.tsx
# 3. Использовать gameStore
```

### Шаг 3: Миграция Dashboard pages

Каждую страницу dashboard нужно мигрировать на studentStore:

```typescript
// Было:
const [profile, setProfile] = useState(null);
const [grades, setGrades] = useState([]);

// Стало:
import { useStore, studentStore } from "@/lib/store";

const profile = useStore(studentStore, (s) => s.profile);
const grades = useStore(studentStore, (s) => s.grades);
const loadProfile = useStore(studentStore, (s) => s.loadProfile);

useEffect(() => {
  loadProfile();
}, []);
```

Использовать shared компоненты:

```typescript
<PageHeader title="Профиль" icon={User} />
<LoadingState message="Загрузка профиля..." />
<EmptyState title="Нет данных" />
<StatCard title="Средний балл" value={profile.averageGrade} icon={Trophy} />
<DataTable data={grades} columns={columns} />
```

### Шаг 4: Миграция Admin pages

Аналогично dashboard, но с adminStore:

```typescript
import { useStore, adminStore } from '@/lib/store'

const students = useStore(adminStore, s => s.students)
const loadStudents = useStore(adminStore, s => s.loadStudents)
const addStudent = useStore(adminStore, s => s.addStudent)

<DataTable
  data={students}
  columns={columns}
  onAdd={addStudent}
  searchKey="name"
/>
```

## 🎯 Checklist для каждого компонента:

### Перед миграцией:

- [ ] Прочитать пример `memory/page_new.tsx`
- [ ] Определить какую логику вынести в shared
- [ ] Определить какие компоненты можно переиспользовать

### Во время миграции:

- [ ] Заменить все useState на store
- [ ] Использовать shared компоненты где возможно
- [ ] Вынести бизнес-логику в отдельные файлы
- [ ] Разбить большие компоненты на маленькие

### После миграции:

- [ ] Убедиться что нет useState для управления состоянием
- [ ] Проверить что все работает
- [ ] Удалить дублирующийся код
- [ ] Обновить импорты

## 📊 Метрики улучшения:

### Было:

- ❌ useState везде (потеря состояния при unmount)
- ❌ Дублирующийся код в каждой игре
- ❌ Большие файлы (1363 строки в chess!)
- ❌ Сложная поддержка

### Стало:

- ✅ Централизованный store (состояние сохраняется)
- ✅ Переиспользуемые компоненты
- ✅ Маленькие файлы (200-300 строк)
- ✅ Легкая поддержка и тестирование

## 🔧 Полезные команды:

```bash
# Проверить размер файлов
wc -l app/dashboard/games/*/page.tsx | sort -rn

# Найти все useState в проекте
grep -r "useState" app/dashboard --include="*.tsx"

# Найти все файлы больше 500 строк
find app -name "*.tsx" -exec wc -l {} \; | awk '$1 > 500 {print}'
```

## 💡 Советы:

1. **Начните с простых игр** (Memory уже готова!)
2. **Используйте готовые примеры** как шаблон
3. **Тестируйте после каждого изменения**
4. **Коммитьте часто** для отслеживания прогресса
5. **Не бойтесь создавать новые shared компоненты**

## 🚨 Важные правила:

1. **НЕ используйте useState** для игровой логики
2. **Всегда используйте store** для состояния
3. **Выносите логику** в отдельные файлы
4. **Создавайте маленькие компоненты** (< 300 строк)
5. **Переиспользуйте** shared компоненты

## 📖 Документация:

- [REFACTORING.md](./REFACTORING.md) - Архитектура и примеры
- [lib/store/types.ts](./lib/store/types.ts) - Все типы
- [shared/index.ts](./shared/index.ts) - Доступные компоненты
- [memory/page_new.tsx](./app/dashboard/games/memory/page_new.tsx) - Пример игры

## 🎉 Готово к использованию!

Вся инфраструктура готова. Осталось только мигрировать компоненты по примеру Memory.

**Каждая миграция займет 30-60 минут** благодаря готовым shared компонентам!
